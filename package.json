{
  "name": "summeca-meme-radar",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Solana meme-token early momentum scanner with paper trading and Telegram alerts.",
  "scripts": {
    "dev": "node src/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.mjs && node --check scripts/birdeye-smoke.mjs && node --test tests/*.test.mjs",
    "smoke:birdeye": "node scripts/birdeye-smoke.mjs"
  },
  "engines": { "node": ">=22" }
}
